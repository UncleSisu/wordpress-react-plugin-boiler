import React from 'react';
import ConstructOption from './ConstructOption';

export default props => (
  <div className='container'>
    <h1>Add Option</h1>
    <ConstructOption option={null} finishEdit={null}/>
  </div>
);
